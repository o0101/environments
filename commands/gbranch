branch="$1"
(git checkout $branch || git checkout -b $branch) && git submodule foreach --recursive 'git checkout '"$branch"' || git checkout -b '"$branch"' || :'
git push --set-upstream origin $branch && git submodule foreach --recursive 'git push --set-upstream origin '"$branch"'
